import { Badge } from '@components/base/badge';

export const MutatingWebhookConfigurationBadge = (): JSX.Element => {
  return (
    <Badge color="mutatingWebhookConfiguration" className='mr-2'>mutatingwebhookconfigurations</Badge>
  )
}