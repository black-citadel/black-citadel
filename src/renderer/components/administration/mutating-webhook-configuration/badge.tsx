import { Badge } from '@protoku-bv/design-system';

export const MutatingWebhookConfigurationBadge = (): JSX.Element => {
  return (
    <Badge variant="purple" className='mr-2'>mutatingwebhookconfigurations</Badge>
  )
}